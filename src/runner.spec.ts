import {expect} from 'chai';
import 'mocha';

describe("fileTransform", function() {
    it("Runs the transform on every file in the glob and returns edits", function() {
        expect(false).to.equal(true);
    });
});
